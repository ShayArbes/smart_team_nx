import { InterceptorInterceptor } from './graphqlCache.interceptor';

describe('InterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new InterceptorInterceptor()).toBeDefined();
  });
});
